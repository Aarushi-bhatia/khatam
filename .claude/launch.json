{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "khatam",
      "runtimeExecutable": "${workspaceFolder}/.venv/bin/uvicorn",
      "runtimeArgs": ["api:app", "--reload", "--port", "8420", "--app-dir", "backend"],
      "port": 8420
    }
  ]
}
